[project]
name = "forge_robots_drivers_core"
version = "0.1.0"
description = "Base robot driver abstractions"
readme = "README.md"
authors = [
    { name = "chipunderblade", email = "pfjhyyj@qq.com" }
]
requires-python = ">=3.12"
dependencies = [
    "forge_robots_core",
]

[tool.uv.sources]
forge_robots_core = { workspace = true }

[build-system]
requires = ["uv_build>=0.8.17,<0.9.0"]
build-backend = "uv_build"
