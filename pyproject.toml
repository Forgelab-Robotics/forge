[project]
name = "forge"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = [
    "packages/msgs",
    "packages/common",
    "packages/configs",
    "packages/robot",
    "packages/robots/*",
    "packages/simulators/*",
]
